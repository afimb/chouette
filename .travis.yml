language: java
jdk:
  - openjdk7
addons:
  postgresql: "9.4"
sudo: true
services:
  - postgresql
install:
  - mvn install -DskipTests -DskipWildfly
before_script:
  - sudo apt-get install postgresql-9.4-postgis-2.3 postgis -y -q
  - psql -c "create user chouette with login createdb superuser encrypted  password 'chouette';" -U postgres
  - psql -c 'create database chouette_test with owner = chouette;' -U postgres
  - psql -c "create extension postgis" -d chouette_test -U postgres 
